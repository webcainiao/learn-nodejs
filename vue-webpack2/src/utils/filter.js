export const formatDate = str => {
  if (!str) {
    return '';
  }
  var date = new Date(str);
  var time = new Date().getTime() - date.getTime();
  if (time < 0) {
    return '';
  } else if () {

  }
}
